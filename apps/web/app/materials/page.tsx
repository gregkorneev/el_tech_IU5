import { Shell } from "../components";

type Material = { id: number; name: string; path: string; type: string; status: string; modifiedAt?: string; originalUrl?: string; excerpt?: string };
async function getMaterials(q?: string): Promise<Material[]> { try { const api = process.env.NEXT_PUBLIC_API_URL ?? "http://localhost:8000"; const result = await fetch(`${api}/materials${q ? `?q=${encodeURIComponent(q)}` : ""}`, { cache: "no-store" }); return result.ok ? result.json() : []; } catch { return []; } }
export default async function Materials({ searchParams }: { searchParams: Promise<{q?: string}> }) { const { q } = await searchParams; const materials = await getMaterials(q); return <Shell><section className="page-head"><p className="eyebrow">Каталог</p><h1>Все материалы</h1><form><input name="q" defaultValue={q} placeholder="Поиск по названию и тексту"/><button>Искать</button></form></section><div className="filter"><span>Все типы</span><span>Любой статус</span><span>Сначала новые</span></div>{materials.length ? <div className="material-list">{materials.map(m => <article key={m.id}><div className="file-icon">{m.type}</div><div><h2>{m.name}</h2><p>{m.path} {m.excerpt && `· ${m.excerpt}`}</p></div><span className={`status ${m.status}`}>{m.status}</span>{m.originalUrl && <a href={m.originalUrl} target="_blank">Открыть ↗</a>}</article>)}</div> : <div className="empty"><h2>{q ? "Ничего не найдено" : "Пока нет синхронизированных материалов"}</h2><p>Каталог заполнится после первого запуска синхронизации.</p><code>python -m worker.sync</code></div>}</Shell>; }
